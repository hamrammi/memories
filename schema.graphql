# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
  query: Query
  mutation: Mutation
}

type Directory {
  id: ID!
  name: String!
  parentId: ID
}

type Memory {
  description: String
  directoryId: ID!
  id: ID!
  title: String!
}

type Mutation {
  createDirectory(name: String!, parentId: ID): Directory
  createMemory(description: String, directoryId: ID!, title: String!): Memory
}

type Query {
  directories: [Directory!]
  memories(directoryId: ID!): [Memory!]
}
